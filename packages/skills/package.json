{
  "name": "@kar-mi/spirit-vale-tools-skills",
  "version": "0.2.0",
  "description": "Build-scoped Spirit Vale skill catalog utilities.",
  "type": "module",
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts --outdir ./dist --target bun --format esm --external @kar-mi/spirit-vale-tools-capture && bunx tsc --project ./tsconfig.build.json"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@kar-mi/spirit-vale-tools-capture": "^1.0.0"
  }
}
