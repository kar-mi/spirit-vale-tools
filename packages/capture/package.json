{
  "name": "@kar-mi/spirit-vale-tools-capture",
  "version": "0.2.3",
  "description": "Passive Windows packet capture and Spirit Vale protocol decoding for Bun.",
  "type": "module",
  "engines": {
    "bun": ">=1.3.0"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "bun build ./src/index.ts ./src/capture.ts ./src/fishnet/wire-reader.ts --outdir ./dist --target bun --format esm && bunx tsc --project ./tsconfig.build.json"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./wire-reader": {
      "types": "./dist/fishnet/wire-reader.d.ts",
      "default": "./dist/fishnet/wire-reader.js"
    },
    "./capture": {
      "types": "./dist/capture.d.ts",
      "default": "./dist/capture.js"
    }
  }
}
