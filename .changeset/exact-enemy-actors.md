---
"@kar-mi/spirit-vale-tools-combat": major
---

Attribute per-enemy damage to stable rendered actor rows instead of reusable network actor IDs. Enemy breakdowns now expose `attackerRowId`, actor snapshots expose `rowId`, and the history schema stores outgoing enemy skills by actor lifetime.
