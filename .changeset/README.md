# Changesets

Add a changeset for every change that affects a published `@kar-mi/spirit-vale-tools-*` package. The release workflow creates a version PR from pending changesets and publishes the updated packages to GitHub Packages after that PR merges.
